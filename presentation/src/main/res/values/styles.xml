<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="RestaurantStatusBar">
        <item name="android:background">@drawable/rounded_corner_background</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">8sp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="RestaurantFavoriteButton">
        <item name="android:adjustViewBounds">true</item>
        <item name="android:background">@null</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:src">@drawable/selector_favorite_star</item>
        <item name="android:padding">@dimen/padding_item_restaurant_favorite_btn</item>
    </style>

    <style name="RestaurantRatingBar" parent="Widget.AppCompat.RatingBar.Small">
        <item name="android:numStars">5</item>
        <item name="android:progressTint">@color/yellow</item>
    </style>
</resources>